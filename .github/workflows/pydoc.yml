name: Generate Pydoc
on: [push]
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pdoc
      - name: Generate Docs
        env:
          DJANGO_SETTINGS_MODULE: my_site.settings
        run: |
          export PYTHONPATH="${PYTHONPATH}:${PWD}"
          # Inicializamos Django correctamente antes de que pdoc toque ningún archivo
          python -c "
          import django, pdoc
          django.setup()
          # Una vez cargado Django, disparamos pdoc de forma totalmente segura
          pdoc.pdoc('blog', 'my_site', 'manage.py', output_directory='docs')
          "
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs
